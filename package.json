{
  "dependencies": {
    "@mui/icons-material": "^5.15.15",
    "@mui/utils": "^5.15.14"
  },
  "scripts": {
    "start": "npm start --prefix server",
    "heroku-postbuild": "cd client && npm install && npm run build && cd ../server && npm install && cd .."
  }
}
